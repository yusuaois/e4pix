# Run with `dart run ffigen --config ffigen.yaml`.
name: E4pixRawBindings
description: |
  Bindings for `src/e4pix_raw.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/e4pix_raw_bindings_generated.dart'
headers:
  entry-points:
    - 'src/e4pix_raw.h'
  include-directives:
    - 'src/e4pix_raw.h'
comments:
  style: any
  length: full
